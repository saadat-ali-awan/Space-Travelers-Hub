const initialState = {};
const missonsReducer = (state = initialState, action) => {
  switch (action.type) {
    case 'ANY_TYPE':
      return state;
    default:
      return state;
  }
};
export default missonsReducer;
